import SecuritySettingsPageClient from '../../security-settings-page-client';

export const dynamic = 'force-dynamic';

export default function SecuritySettingsPage() {
  return <SecuritySettingsPageClient />;
}
